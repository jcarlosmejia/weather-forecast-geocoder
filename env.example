# Environment Variables for Weather Forecast Geocoder
# Copy this file to .env.local to customize API endpoints

# Geocoding API URL (optional)
# Default: /api/geocoding/geocoder/locations/onelineaddress (dev) or https://geocoding.geo.census.gov/geocoder/locations/onelineaddress (prod)
# For backup CORS proxy: http://localhost:8080/geocoding/geocoder/locations/onelineaddress
# VITE_GEOCODING_API_URL=http://localhost:8080/geocoding/geocoder/locations/onelineaddress

# Weather API URL (optional)
# Default: /api/weather (dev) or https://api.weather.gov (prod)
# For backup CORS proxy: http://localhost:8080/weather
# VITE_WEATHER_API_URL=http://localhost:8080/weather

# Development Notes:
# - These variables are only needed if the default Vite proxy doesn't work
# - The backup CORS proxy server is provided as cors-proxy-server.js
# - In production, you'll need your own backend proxy or serverless functions